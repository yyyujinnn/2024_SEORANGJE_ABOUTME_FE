version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: /dist # Vite의 기본 빌드 디렉토리
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
  redirects:
    - source: </^((?!\.).)*$/>
      target: /index.html
      status: 200
