version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: /dist # 빌드된 파일들이 저장되는 디렉토리 (프로젝트 설정에 맞게 변경)
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
